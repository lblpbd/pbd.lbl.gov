{% extends 'layout/base.twig' %}
{% block content %}
	<section id="primary" class="content-area">
		<main id="main" class="site-main" role="main">

		{% if ( have_posts() ) %}

			<header class="page-header">
				<h1 class="page-title">{# printf( __( 'Search Results for: %s', 'bioscience' ), '<span>' . get_search_query() . '</span>' ); #}</h1>
			</header><!-- .page-header -->

			{# while ( have_posts() ) : the_post() #}

				{# get_template_part( 'content', 'search' ) #}

			{# endwhile #}

			{# bioscience_content_nav( 'nav-below' ) #}

		{% else %}

			{# get_template_part( 'no-results', 'search' ) #}

		{% endif %}

		</main><!-- #main -->
	</section><!-- #primary -->

{% endblock %}